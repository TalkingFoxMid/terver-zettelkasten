[[Случайная величина]] $\xi$ имеет дискретное [[Распределение СВ]], если существует конечный набор чисел $A = \{a_1,...,a_n\}$, такой, что:
$\sum_{i=1}^{n}{P(\xi = a_i)}=1$

Если СВ имеет дискретное распределение, то для любого $B \subset A$:
$$P(\xi \in B) = \sum_{a_i \in B}{P(a_i = \xi)}$$